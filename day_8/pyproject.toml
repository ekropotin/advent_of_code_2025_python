[project]
name = "day-8"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "scipy>=1.16.3",
]

[tool.pyright]
typeCheckingMode = "strict"

[tool.uv.workspace]
members = [
    "day_9",
]
